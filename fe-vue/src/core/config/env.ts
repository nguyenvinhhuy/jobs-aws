export const env = {
  // Empty baseURL: all API paths are absolute (e.g. '/api/public/home')
  // They go directly to the current origin so nginx can proxy /api/ → backend correctly
  apiBaseUrl: '',
}
